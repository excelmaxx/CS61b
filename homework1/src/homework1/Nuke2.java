package homework1;

import java.io.*;

public class Nuke2 {
	/*
	 * 
	 */
	public static void main(String[] args) throws Exception{
	  
		BufferedReader keyboard = null;
	    String inputLine;
		
	    inputLine = keyboard.readLine();
		
	    String newLine;
	    newLine = inputLine.substring(0)+inputLine.substring(1, (inputLine.length()-1));
		System.out.println(newLine);
		
			
	}
}
